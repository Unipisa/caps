const Proposal = require('./models/Proposal')
const mongoose = require('mongoose')

module.exports= async () => {
    // this code will be executed at server start, after
    // database has been initialized
}