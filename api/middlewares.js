export function authViewer(res, req, next) {
    // Todo: check if the user is authorised to view
    // private content
    next()
}


export function authEditor(res, req, next) {
    // Todo: check if the user is authorised to edit
    // private content
    next()
}
